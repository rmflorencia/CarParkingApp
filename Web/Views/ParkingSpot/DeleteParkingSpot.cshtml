<form asp-action="DeleteParkingSpot" role="form">
    @Html.Partial("_ModalHeader", new ModalHeader { Heading = "Delete Parking Spot" })

    <div class="modal-body form-horizontal">
        Are you sure you want to delete Parking Spot @Model?
    </div>
    @Html.Partial("_ModalFooter", new ModalFooter { SubmitButtonText = "Delete" })
</form>
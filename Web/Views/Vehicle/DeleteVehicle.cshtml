<form asp-action="DeleteParkingLot" role="form">
    @Html.Partial("_ModalHeader", new ModalHeader { Heading = "Delete Parking Lot" })

    <div class="modal-body form-horizontal">
        Are you sure Vehicle @Model is leaving?
    </div>
    @Html.Partial("_ModalFooter", new ModalFooter { SubmitButtonText = "Delete" })
</form>